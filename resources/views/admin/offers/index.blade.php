@extends('layouts.admin')
@section('content')
<div class="content"><div class="panel panel-default"><div class="panel-heading">Online payment offers <a href="{{ route('admin.offers.create') }}" class="btn btn-xs btn-success pull-right">Add offer</a></div><div class="panel-body table-responsive"><table class="table table-bordered table-striped"><thead><tr><th>Title</th><th>Discount</th><th>Minimum order</th><th>Period</th><th>Status</th><th></th></tr></thead><tbody>@forelse($offers as $offer)<tr><td>{{ $offer->title }}</td><td>{{ $offer->discount_percent }}%</td><td>₹{{ number_format($offer->minimum_order_amount,2) }}</td><td>{{ optional($offer->starts_at)->format('d M Y') ?: 'Now' }} – {{ optional($offer->ends_at)->format('d M Y') ?: 'No expiry' }}</td><td>{{ $offer->is_active ? 'Active' : 'Inactive' }}</td><td><a class="btn btn-xs btn-info" href="{{ route('admin.offers.edit',$offer) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.offers.destroy',$offer) }}">@csrf @method('DELETE')<button class="btn btn-xs btn-danger" onclick="return confirm('Delete this offer?')">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center">No offers created.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
