@extends('layouts.frontend')
@section('frontend-content')
<style>.credit-list{background:#f5f7fb;padding:32px 0;min-height:60vh}.credit-list .card{border:0;border-radius:15px;box-shadow:0 10px 25px rgba(18,34,56,.07)}.credit-list .card-header{background:#fff;border-bottom:1px solid #e7edf5;padding:20px;font:800 20px 'Barlow Condensed',sans-serif}.credit-list .table th{font-size:11px;text-transform:uppercase;letter-spacing:.05em;color:#718096;border-top:0}.credit-list .table td{vertical-align:middle}.credit-status{padding:5px 9px;border-radius:20px;font-size:11px;font-weight:800}.credit-status.Pending{background:#fff4df;color:#ad6800}.credit-status.Active{background:#e9faf4;color:#087f5b}.credit-status.Inactive,.credit-status.Suspended{background:#fff0f0;color:#c92a2a}</style>
<section class="credit-list"><div class="container"><div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="mb-1" style="font-family:'Barlow Condensed';font-weight:800">Wallet & credit</h2><small class="text-muted">Your credit applications and approved limits</small></div><a class="btn btn-primary" style="background:#3566e8;border:0;border-radius:7px;font-weight:700" href="{{ route('frontend.wallet-requests.create') }}"><i class="fa-solid fa-plus me-1"></i> Apply for credit</a></div>@if(session('message'))<div class="alert alert-info">{{ session('message') }}</div>@endif<div class="card"><div class="card-header">Credit line history</div><div class="card-body p-0"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Application</th><th>Requested / approved amount</th><th>Status</th><th>Applied on</th><th></th></tr></thead><tbody>@forelse($walletRequests as $walletRequest)<tr><td>#{{ $walletRequest->id }}</td><td>₹ {{ number_format($walletRequest->welcome_amount ?? 0,2) }}</td><td><span class="credit-status {{ $walletRequest->status }}">{{ App\Models\WalletRequest::STATUS_SELECT[$walletRequest->status] ?? $walletRequest->status }}</span></td><td>{{ optional($walletRequest->created_at)->format('d M Y') }}</td><td>@if($walletRequest->status === 'Active')<a class="btn btn-sm btn-primary" href="{{ route('frontend.wallet.statement') }}">View statement</a>@endif</td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted"><i class="fa-solid fa-wallet d-block mb-2" style="font-size:28px"></i>No credit application yet. Apply when you need a business credit line.</td></tr>@endforelse</tbody></table></div></div></div></div></section>
@endsection
